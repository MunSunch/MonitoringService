package com.munsun.monitoring_service.backend.security;

import javax.servlet.ServletRequest;
import javax.servlet.ServletResponse;

public class SecurityFilterImpl implements SecurityFilter{
    @Override
    public void isAllowed(ServletRequest servletRequest, ServletResponse servletResponse) {

    }
}
